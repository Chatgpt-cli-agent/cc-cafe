/**
 * Single place to bump the user-visible build label.
 * Keep APP_VERSION in sync with app/package.json "version".
 */
export const APP_VERSION = '0.6.1';
export const BUILD_CODE = 'S4MM-UI-2';
export const BUILD_LABEL = `v${APP_VERSION} (${BUILD_CODE})`;
